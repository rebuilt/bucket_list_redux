<!-- title count img_url  -->
<div class="card">
  <div class="card__thumb">
    <%= link_to idea do  %>
      <img src="<%= idea.photo_url  %>"
           alt="Northern lights over mountain covered in snow">
           <div class="card__intro">
             <h4 class="card__title"><%= idea.title  %>  </h4>
             <span><%= idea.done_count  %> have done this</span>
           </div>
      </img>
    <% end %>
  </div>
  <div class="card__footer">
    <button class="card__btn card__like delta" data-like="see-the-northern-lights">
      <svg class="icon-heart icon-heart--liked" role="presentation">
        <use xlink:href="<%= asset_url("icons-defs.svg" + "#heart") %>"></use>
      </svg>
      Like
    </button>
    <% if logged_in?  %>
      <% if has_goal?(idea)  %>
        <%= render(partial: "remove_goal_btn", locals: {goal: idea})  %>
      <% else  %>
        <%= render(partial: "add_goal_btn", locals: {goal: idea}  )%>
      <% end %>
    <% end %>
  </div>
</div>
