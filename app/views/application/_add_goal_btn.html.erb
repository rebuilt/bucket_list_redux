<%= button_tag(type: "submit", class: 'btn delta', disabled: @disable_add_goal) do %>
  <svg class="icon-heart icon--sm" role="presentation">
    <use xlink:href="<%= asset_path("icons-defs.svg#heart")%>"></use>
  </svg>
  <% if @disable_add_goal  %>
    Added
  <% else  %>
    Add Goal
  <% end %>
<% end %>
